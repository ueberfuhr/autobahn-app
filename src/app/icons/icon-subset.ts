import {
  cilFolder,
  cilMenu,
  cilWarning
} from '@coreui/icons';

export const iconSubset = {
  cilFolder,
  cilMenu,
  cilWarning
};

export enum IconSubset {
  cilFolder = 'cilFolder',
  cilMenu = 'cilMenu',
  cilWarning = 'cilWarning'
}
